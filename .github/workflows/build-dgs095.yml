name: Build DGS 0.9.5
on:
  push:
    branches: [dragon-game-studio-0.9.5-build]
    paths:
      - dgs095-source.zip.b64
      - ci-build-dgs095.sh
      - .github/workflows/build-dgs095.yml
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - run: bash ci-build-dgs095.sh
      - uses: actions/upload-artifact@v4
        with:
          name: DragonGameStudio-0.9.5-original-signature
          path: dgs095/DragonGameStudio-0.9.5-debug.apk
          if-no-files-found: error
          retention-days: 7
